# Copy to .env — never commit the real .env file.

MONAD_TESTNET_RPC=https://testnet-rpc.monad.xyz

# The signer address configured as the contract's oracle. Only this key can
# call submitVerification. Keep it separate from the deployer/provider keys.
ORACLE_PRIVATE_KEY=

# Address of the deployed ProvoMarketplace contract.
CONTRACT_ADDRESS=
